--- a/ftp/cmds.c
+++ b/ftp/cmds.c
@@ -1049,9 +1049,9 @@ checkglob(int fd, const char *pattern)
 }
 
 static const char *
-onoff(int bool)
+onoff(int opt)
 {
-	return (bool ? "on" : "off");
+	return (opt ? "on" : "off");
 }
 
 /*
